rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 단계: 모든 읽기/쓰기 허용
    // TODO: 프로덕션 전에 보안 규칙 강화 필요
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
